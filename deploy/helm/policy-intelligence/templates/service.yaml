apiVersion: v1
kind: Service
metadata:
  name: policy-intelligence
  labels:
    app: policy-intelligence
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  selector:
    app: policy-intelligence
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
